#include <iostream>
using namespace std;
int main(){
	int a,b,c;
	cin>>a>>b>>c;
	if (c>a && c>b ){
	cout <<" c la so lo nhat";
    }else if(b>a && b>c){
	cout<<"b la so lo nhat";
    }else if(a>>b && a>>c){
	cout<<"a la so lon nhat";
    }   
    return 0;
} 
